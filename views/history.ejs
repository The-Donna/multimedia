<!DOCTYPE html>
<html>
<head>
  <title>Your History</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; background: #fdfaf4; color: #333; }
    h1 { color: #578E7E; }
    ul { list-style-type: none; padding: 0; }
    li { margin: 10px 0; border-bottom: 1px solid #ccc; padding-bottom: 5px; }
  </style>
</head>
<body>
  <h1>Your Viewing History</h1>
  <% if (history.length === 0) { %>
    <p>No history found yet.</p>
  <% } else { %>
    <ul>
      <% history.forEach(item => { %>
        <li><strong><%= item.title %></strong> (<%= item.type %>) - <%= item.dateViewed.toDateString() %></li>
      <% }) %>
    </ul>
  <% } %>
  <a href="/Books&Co/Index.html">← Back to Home</a>
</body>
</html>
